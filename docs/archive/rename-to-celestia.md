# Was “Atlas”

Project folder is `C:\celestia`. Code package is `celestia_core`. Entry script is `run_celestia.py`.

`app.user_id: atlas_user` in config was left alone so old Chroma memories didn’t need a migration.
