# Copy this file to .env in the same folder (c:\atlas\.env)
# .env is gitignored — safe for secrets.

# HuggingFace token (optional but recommended)
# Used when Orpheus TTS downloads the SNAC audio decoder on first speak.
# Create: https://huggingface.co/settings/tokens → New token → Read
HF_TOKEN=
